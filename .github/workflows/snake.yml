name: Generate Snake

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */12 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Generate Snake Animation
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: saireddyganapuram
          outputs: snake.svg

      - name: Push Animation
        uses: EndBug/add-and-commit@v7
        with:
          add: "snake.svg"
          message: "Generated snake animation"
          branch: output
